name: githab-actions
on: push
jobs:
        my-job:
                runs-on: ubuntu-latest
                steps: 
                        - name: my-step
                          run:
                                pip install poetry
                                make lint